"use strict";
/**
 * This is the only file you need to modify
 */
Object.defineProperty(exports, "__esModule", { value: true });
const bundle_analyzer_1 = require("@perfsee/bundle-analyzer");
const exampleAudit = ({ chunks }) => {
    const initialAssets = new Set(chunks
        .filter((chunk) => !chunk.async)
        .map((chunk) => chunk.assets.map((asset) => asset.name))
        .flat());
    return {
        id: 'http2-notice',
        title: 'Use HTTP/2',
        desc: 'HTTP/2 offers many benefits over HTTP/1.1, including binary headers, multiplexing, and server push.',
        link: 'https://http2.github.io/faq/',
        score: initialAssets.size <= 6 ? bundle_analyzer_1.BundleAuditScore.Good : bundle_analyzer_1.BundleAuditScore.Notice,
        weight: 0,
    };
};
// Keep default export
exports.default = exampleAudit;
//# sourceMappingURL=audit.js.map