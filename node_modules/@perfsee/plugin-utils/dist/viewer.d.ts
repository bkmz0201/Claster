import { BundleResult, ModuleReasons, ModuleTreeNode } from '@perfsee/bundle-analyzer';
export declare const PACKAGE_NAME = "@perfsee/plugin-utils";
interface Data {
    branch: string;
    hash: string;
    report: BundleResult;
    content: ModuleTreeNode[];
    moduleReasons?: ModuleReasons;
}
export interface ReportOptions {
    /**
     * Automatically open report in default browser.
     *
     * @default true
     */
    openBrowser?: boolean;
    /**
     * Path to bundle report file that will be generated.
     * It can be either an absolute path or a path relative to a bundle output directory.
     *
     * By default the report will be output in the cache directory.
     */
    fileName?: string;
}
export declare function renderReportViewer(data: Data): string;
export declare function saveReport(data: Data, outputPath: string, { fileName, openBrowser }?: ReportOptions): Promise<string | undefined>;
export {};
