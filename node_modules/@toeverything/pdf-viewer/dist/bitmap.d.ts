import { BitmapFormat } from '@toeverything/pdf-viewer-types';
import type { Runtime } from './runtime.js';
export declare class Bitmap {
    runtime: Runtime;
    private ptr;
    format: BitmapFormat;
    static format: BitmapFormat;
    constructor(runtime: Runtime, ptr: number, format?: BitmapFormat);
    get pointer(): number;
    close(): void;
    fill(left: number, top: number, width: number, height: number, color?: number): number;
    toUint8Array(): Uint8Array;
    [Symbol.dispose](): void;
}
//# sourceMappingURL=bitmap.d.ts.map