import { PerfseeReportStats } from '@perfsee/bundle-analyzer';
import { CommonPluginOptions } from './options';
export interface BuildUploadParams {
    projectId: string;
    host: string;
    namespace: string;
    name: string;
    artifactName: string;
    branch: string;
    commitHash: string;
    commitMessage?: string;
    tag?: string;
    appVersion?: string;
    nodeVersion?: string;
    toolkit?: string;
    author?: string;
    pr?: {
        number: number;
        baseHash: string;
        headHash: string;
    };
}
export declare class BuildUploadClient {
    private readonly options;
    private readonly outputPath;
    private readonly appVersion;
    constructor(options: CommonPluginOptions, outputPath: string, appVersion: string);
    uploadBuild(stats: PerfseeReportStats): Promise<void>;
    private writeStats;
    private pack;
    private uploadPack;
    /**
     * find the topest level of all the assets
     */
    private getRealOutputPath;
}
