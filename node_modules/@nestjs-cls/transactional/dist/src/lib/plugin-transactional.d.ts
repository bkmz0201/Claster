import { Provider } from '@nestjs/common';
import { ClsPlugin } from 'nestjs-cls';
import { TransactionalAdapter, TransactionalPluginOptions } from './interfaces';
export declare class ClsPluginTransactional implements ClsPlugin {
    name: string;
    providers: Provider[];
    imports: any[];
    exports: any[];
    constructor(options: TransactionalPluginOptions<any, any, any>);
    private bindLifecycleHooks;
}
export declare class TransactionProxyUnsupportedError extends Error {
    constructor(adapter: TransactionalAdapter<any, any, any>);
}
//# sourceMappingURL=plugin-transactional.d.ts.map