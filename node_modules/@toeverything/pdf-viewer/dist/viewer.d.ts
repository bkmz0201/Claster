import type { Runtime } from './runtime.js';
import { Document } from './document.js';
import { Bitmap } from './bitmap.js';
export declare class Viewer {
    runtime: Runtime;
    constructor(runtime: Runtime);
    /**
     * Opens and load a PDF document from memory.
     */
    open(bytes: Uint8Array, password?: string): Document | undefined;
    /**
     * Opens and load a PDF document from memory with Blob format.
     */
    openWithBlob(blob: Blob, password?: string): Promise<Document | undefined>;
    /**
     * Creates an empty bitmap.
     */
    createBitmap(width: number, height: number, alpha: number): Bitmap;
    /**
     * Creates a bitmap with params.
     */
    createBitmapWith(width: number, height: number, format?: import("@toeverything/pdf-viewer-types").BitmapFormat, firstScan?: number, stride?: number): Bitmap;
    close(): void;
    [Symbol.dispose](): void;
}
//# sourceMappingURL=viewer.d.ts.map