import { Logger } from './stats-parser/types';
export declare const installActivatedRunnerScript: (logger: Logger, fetch: (url: string, init?: RequestInit) => Promise<any>, cacheDir?: string) => (jobType: string) => Promise<string | undefined>;
